global:
  scrape_interval: 15s
  scrape_timeout: 5s

rule_files:
  - alerts.example.yml

scrape_configs:
  - job_name: go-sync
    metrics_path: /metrics
    static_configs:
      - targets: ["127.0.0.1:9108"]
        labels:
          source: example
